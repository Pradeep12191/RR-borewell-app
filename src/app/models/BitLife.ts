export interface BitLife {
    bit_no: number,
    date: string,
    id: number,
    rpm_no: number,
    running_feet: number,
    serial_no: number,
    total_feet: number,
    vehicle_id: number,
    vehicle_no: string
}